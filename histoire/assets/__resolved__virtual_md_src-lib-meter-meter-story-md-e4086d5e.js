const html = `<h1 id="meter-component-implementation" tabindex="-1">Meter component implementation <a class="header-anchor" href="#meter-component-implementation" aria-hidden="true">#</a></h1>
<p>Test task for Haut AI Front-end position</p>
<h2 id="component-props" tabindex="-1">Component props: <a class="header-anchor" href="#component-props" aria-hidden="true">#</a></h2>
<ul>
<li><strong>min</strong> - minimum value</li>
<li><strong>max</strong> - maximum value</li>
<li><strong>optimum</strong> - value, that changes active state for icons, when <strong>value</strong> prop hits it</li>
<li><strong>value</strong> - value, that Meter component shows</li>
</ul>
<h2 id="size" tabindex="-1">Size <a class="header-anchor" href="#size" aria-hidden="true">#</a></h2>
<p>The meter component changes its size to fit different layouts. It's adaptive to its parent container and change its visual representation at 320px width breakpoint.</p>
<p>You can see these visual changes in the component variants.</p>
`;
const frontmatter = {};
const relativePath = "src/lib/Meter/Meter.story.md";
export {
  frontmatter,
  html,
  relativePath
};
